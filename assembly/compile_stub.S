# THIS IS A FILE GENERATED BY meta_assembly_compile.py APPLIED ON compile_stub_m.S
# ASSUME s1 holds a pointer to the pcb
# ASSUME s2 holds a pointer to argument pointer array for all instr
# ASSUME s3 holds a pointer to the argument array for this instr
# ASSUME s4 holds a pointer to the array of all callable cxx funs
# ASSUME s5 holds a pointer to the X register array 

# Assume:
# - s9 has the pointer to the jump ptr
# (the above must be computed if you want to even jump here)
# - a1 has the label number
ld a0, 24(s1) # generated from 'load_shared a0, CODE_CHUNK_P'

ld t0, 8(s4) # generated from 'call_cxx_fun COMPILE_LABEL'
jalr t0 # cont...
# set value in table to point to label loc
sd a0, 0(s9)

# then jump 
jr a0
