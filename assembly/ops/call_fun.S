# THIS IS A FILE GENERATED BY meta_assembly_compile.py APPLIED ON ops/call_fun_m.S
# ASSUME s1 holds a pointer to the pcb
# ASSUME s2 holds a pointer to argument pointer array
# ASSUME s3 holds a pointer to the argument array for this instr

# save ra
addi sp, sp, -8
sd ra, 0(sp)

# call
ld t0, 8(s3) # generated from 'load_arg t0, 1'
jalr t0

# restore ra
ld ra, 0(sp)
addi sp, sp, 8

ret
